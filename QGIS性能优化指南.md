# QGIS 画布卡顿优化指南

## 一、高分辨率屏幕优化

### 问题诊断
- **4K 屏幕 (3840×2160)** → QGIS 每次渲染像素量是 1080p 的 4 倍
- **2K 屏幕 (2560×1440)** → 像素量是 1080p 的 2.3 倍
- **多栅格图层** → 每一层都要重新采样到屏幕分辨率

### 解决方案 1：降低 QGIS 显示分辨率（推荐）

#### 方法 A：修改 Windows 缩放（治本）
1. 右键桌面 → 显示设置
2. 缩放与布局 → 150% 或 125%
3. **关键**：高级缩放设置 → 自定义缩放 → 125%
4. 注销重新登录

#### 方法 B：单独为 QGIS 设置兼容性（治标）
1. 找到 `qgis-bin.exe`（通常在 `C:\Program Files\QGIS 3.xx\bin\`）
2. 右键 → 属性 → 兼容性
3. 勾选"替代高 DPI 缩放行为"
4. 缩放执行：**应用程序**
5. 点击"更改高 DPI 设置"
6. 勾选"替代高 DPI 缩放行为，缩放执行者：系统"
7. 确定并重启 QGIS

**效果**：画布分辨率降低 → 渲染速度提升 2-4 倍

---

## 二、栅格图层优化

### 1. 创建金字塔（必做）
```
设置 → 选项 → 渲染 → 栅格金字塔
- 格式：内部（如果有写入权限）或外部 .ovr
- 重采样：平均或最近邻
```

**手动创建金字塔**：
```
右键栅格图层 → 属性 → 金字塔 → 全选级别 → 构建金字塔
```

### 2. 调整渲染质量
```
设置 → 选项 → 渲染 → 渲染质量
- 地图画布：快速（降低抗锯齿）
- 屏幕分辨率：72 dpi（不要设置太高）
```

### 3. 关闭不必要的渲染效果
```
设置 → 选项 → 渲染
- ❌ 取消勾选"渲染图层时使用渲染缓存"（大栅格反而更慢）
- ✅ 勾选"仅在停止平移和缩放后渲染"
- ✅ 勾选"简化几何要素"
```

### 4. 设置栅格重采样方法
```
右键栅格图层 → 属性 → 符号系统 → 重采样
- 放大：双线性或最近邻
- 缩小：平均
```

---

## 三、显卡与硬件加速

### 1. 启用 OpenGL 渲染（推荐）
```
设置 → 选项 → 渲染
- 勾选"使用 OpenGL 渲染"
- 重启 QGIS
```

### 2. 检查显卡驱动
```
- 打开 NVIDIA 控制面板或 AMD Radeon 软件
- 更新到最新驱动
- 设置 qgis-bin.exe 为高性能模式
```

### 3. NVIDIA 显卡优化
```
NVIDIA 控制面板 → 管理 3D 设置 → 程序设置
- 添加 qgis-bin.exe
- 电源管理模式：最高性能优先
- 首选图形处理器：高性能 NVIDIA 处理器
- 纹理筛选 - 质量：高性能
- 三重缓冲：关
```

---

## 四、系统性能优化

### 1. 高优先级启动（已创建脚本）
```
双击 "启动QGIS高优先级.bat"
```

### 2. 增加 QGIS 内存限额
```
设置 → 选项 → 系统
- 最大线程数：CPU 核心数 - 1
- 日志消息：仅错误（减少 I/O）
```

### 3. 关闭后台程序
```
- 关闭 Chrome/Edge（占用大量内存）
- 关闭微信/QQ/钉钉等聊天工具
- 关闭 360/腾讯管家等安全软件实时保护
```

---

## 五、图层管理策略

### 1. 栅格图层分组控制
```
- 将底图栅格单独分组
- 只显示当前工作区域的栅格
- 使用"图层可见性控制"快速切换
```

### 2. 使用虚拟栅格（VRT）
```python
# 合并多个栅格为一个 VRT 文件
处理 → 工具箱 → 栅格常用操作 → 构建虚拟栅格

优点：
- 单个文件管理多个栅格
- 按需加载，不占用内存
- 支持金字塔加速
```

### 3. 裁剪栅格到工作区域
```
处理 → 工具箱 → GDAL → 栅格提取 → 按范围裁剪栅格
- 只保留当前作业区域
- 减少数据量 80% 以上
```

---

## 六、快速诊断命令

### 检查当前分辨率
```
Python 控制台输入：
from PyQt5.QtWidgets import QApplication
screen = QApplication.primaryScreen()
print(f"分辨率: {screen.size().width()}×{screen.size().height()}")
print(f"DPI 缩放: {screen.devicePixelRatio()}")
```

### 检查栅格图层信息
```
Python 控制台输入：
layer = iface.activeLayer()
if layer.type() == 0:  # 栅格
    print(f"大小: {layer.width()}×{layer.height()}")
    print(f"金字塔: {layer.dataProvider().hasPyramids()}")
```

---

## 七、终极优化方案（组合拳）

### 方案 A：日常作业（推荐）
1. ✅ 设置 QGIS 兼容性为"系统缩放"
2. ✅ 所有栅格创建金字塔
3. ✅ 启用 OpenGL 渲染
4. ✅ 高优先级启动
5. ✅ 只显示当前区域栅格

**预期效果**：卡顿减少 70-80%

### 方案 B：极限性能（高分辨率屏幕）
1. ✅ Windows 系统缩放改为 125%
2. ✅ QGIS 兼容性设置"系统缩放"
3. ✅ 所有栅格转为金字塔 VRT
4. ✅ NVIDIA 显卡高性能模式
5. ✅ 实时优先级启动
6. ✅ 关闭所有后台程序

**预期效果**：卡顿减少 90% 以上

### 方案 C：买硬件（终极方案）
- 升级显卡：RTX 3060 以上
- 升级内存：32GB 以上
- 升级硬盘：NVMe SSD
- 或者：外接 1080p 显示器专门跑 QGIS

---

## 八、常见问题排查

### Q1: 改了兼容性还是卡？
A: 检查是否真正生效
```
任务管理器 → 详细信息 → qgis-bin.exe → 右键 → DPI 识别
应该显示"系统"而不是"每监视器 v2"
```

### Q2: 创建金字塔后反而更卡？
A: 可能是"内部金字塔"创建失败
```
改用"外部 .ovr 金字塔"：
图层属性 → 金字塔 → 金字塔格式：外部（GeoTIFF .ovr）
```

### Q3: OpenGL 启用后花屏？
A: 显卡驱动太老或不兼容
```
1. 更新显卡驱动到最新
2. 或者关闭 OpenGL，改用 CPU 渲染
```

### Q4: 高优先级脚本没效果？
A: 可能需要管理员权限
```
右键脚本 → 以管理员身份运行
```

---

## 九、验证优化效果

### 测试方法
1. 加载 10 个大栅格图层
2. 快速拖动画布 10 秒
3. 观察是否流畅

**优化前**：严重卡顿，拖不动
**优化后**：基本流畅，偶尔延迟

---

## 总结：最快见效的 3 步

1. **QGIS 兼容性设置为"系统缩放"**（5 秒完成，效果立竿见影）
2. **所有栅格创建金字塔**（10 分钟完成，效果显著）
3. **高优先级启动**（双击脚本，即时生效）

如果还是卡，那真的是分辨率太高了，建议：
- 外接 1080p 显示器专门跑 QGIS
- 或者把栅格数据裁剪到工作区域
