## 🐛 Bug 修复

**修复添加要素带预设功能的严重问题**：
- 解决第一个添加的要素属性被后续要素覆盖的问题
- 解决保存图层时出现「无法将要素 ID -9223372036854775808 升级」错误
- 解决编辑缓冲区 feature ID 冲突导致的数据混淆

## 🔧 技术改进

**三层防护机制**：
1. **唯一随机临时 ID**：为每个 feature 分配随机负数 ID（避免 QGIS 内部冲突）
2. **JSON 序列化**：彻底断开 C++/Qt 层的对象引用，避免属性污染
3. **生命周期管理**：Controller 持有 features 列表，防止 Python 过早回收

**保留用户友好性**：
- ✅ 仍然使用编辑缓冲区，支持撤销操作
- ✅ 不强制立即保存，用户可以批量编辑后统一保存

## 📦 安装方法

1. 下载 `lanebatchupdate_v1.0.4.82_20260906_154053.zip`
2. 解压后双击 `install.bat`（Windows）
3. 重启 QGIS

## 🧪 测试建议

1. 连续添加多个要素，每个使用不同的预设
2. 保存图层后检查属性表，确认每个要素属性正确
3. 不应再出现「无法升级 ID」的错误

---

**完整更新日志**：参见 commit a751c3e
