# Lane 批量刷值工具 v1.0.4.68 更新日志

## 新增功能

### ✨ Js2jd 转换功能
- 新增 **Js2jd 转换** 按钮，将 51N 通用规格转换为京东规格
- 自动识别当前 QGIS 工程中加载的 shapefile 图层
- 转换前自动创建带时间戳的备份
- 自动清理 10 天前的旧备份
- 转换后自动删除不需要的关系文件（CROSSWALK_LANE_REL、INTERSECTION_SIGNAL_REL）
- 完成后自动打开输出文件夹

## 使用说明

### 前置条件
1. 在 QGIS 中加载需要转换的 18 个 shapefile 图层
2. 确保以下 13 个必需图层已加载：
   - boundary
   - lane
   - lane_node
   - road
   - lane_section
   - intersection
   - crosswalk
   - stopline
   - signal
   - speedbump
   - gate
   - safetyisland
   - prohibited_area

### 操作步骤
1. 在 QGIS 中加载所有图层（例如从 `d:\Desk\太仓51N-模板` 导入）
2. 点击工具栏中的 **Js2jd 转换** 按钮（文件夹+转换箭头图标）
3. 等待转换完成
4. 检查输出文件夹中的转换结果

### 输出说明
- 原始文件备份：`{源目录}/../备份/{时间戳}/`
- 转换结果：`{源目录}/output/`
- 自动删除的文件：
  - CROSSWALK_LANE_REL.shp/.shx/.prj/.fix/.dbf
  - INTERSECTION_SIGNAL_REL.shp/.shx/.prj/.fix/.dbf

### 转换内容对照表

| 输入图层 | 输出图层 | 主要变化 |
|---------|---------|---------|
| boundary | LANE_MARKING | ID→LANEMARKID, COLOR→HEIGHT |
| lane | LANE | 字段重命名+部分计算字段 |
| lane_node | LANE_NODE | 字段映射 |
| road | ROAD_LINK | 字段映射 |
| lane_section | LANE_GROUP | 字段映射 |
| intersection | INTERSECTION | 字段映射 |
| signal | TRAFFIC_LIGHT | 字段映射 |
| crosswalk | CROSSWALK | 字段映射 |
| stopline | STOPLINE | 字段映射 |
| speedbump | SPEEDBUMP | 字段映射 |
| gate | GATE | 字段映射 |
| safetyisland | SAFETYISLAND | 字段映射 |
| prohibited_area | PROHIBITED_AREA | 字段映射 |

## 技术细节

### 依赖工具
- Groovy 运行环境
- GeoTools 10.0 库（位于 E:\tool\js2data\lib）
- Js2jd.groovy 转换脚本（位于 E:\tool\js2data\src\java）

### 转换原理
1. 读取输入 shapefile 的字段和几何
2. 执行字段映射和值转换
3. 创建符合京东规格的输出 shapefile
4. 填充固定值和计算字段

## 版本信息
- 版本号：v1.0.4.68
- 发布日期：2026-08-28
- 兼容 QGIS 版本：3.0+

## 注意事项
- 转换工具仅做**格式转换和字段映射**
- **不包含**路口几何生成、车道关系计算等复杂逻辑
- 确保 Groovy 和相关库已正确安装
- 转换时间取决于数据量大小
